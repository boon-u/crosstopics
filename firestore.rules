rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open for this classroom board tool.
    // Firebase Console → Firestore → Rules → Publish
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
