rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Classroom session board — open for this training tool.
    // Tighten later if you add real user accounts.
    match /boards/{boardId} {
      allow read, write: if true;
    }
  }
}
